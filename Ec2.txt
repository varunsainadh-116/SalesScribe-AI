    yum install git
    git colne https://github.com/varunsainadh-116/SalesScribe-AI.git

    ###########################
    cd ~/SalesScribe-AI  # Navigate to your project folder
    python3 -m venv venv  # Create a virtual environment
    source venv/bin/activate  # Activate the virtual environment

    pip install --upgrade pip

    pip install langchain==0.2.14 langchain-community==0.2.12 langchain-groq===0.1.9 \
        unstructured==0.14.6 selenium==4.21.0 chromadb==0.5.0 \
        streamlit==1.35.0 pandas==2.0.2 python-dotenv==1.0.0

    pip freeze | grep -E "langchain|unstructured|selenium|chromadb|streamlit|pandas|python-dotenv"

    pstree -ap | grep python
    pstree -ap | grep streamlit

    nohup python3 -m streamlit run app/main.py



